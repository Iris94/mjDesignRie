---
import Icon from './Icon.astro';
import { LINKS } from '../constants/links';
import { ICON_COLORS } from '../constants/icons';

const socials = ['instagram', 'tiktok', 'spotify', 'youtube', 'appleMusic'] as const;
---

<footer class="bg-surface border-t border-surface-variant py-8 relative z-10">
  <div class="px-4 md:px-grid-margin flex flex-col gap-6">
    <div class="text-2xl font-headline font-black italic text-surface-variant">RIE</div>

    <div class="flex flex-col gap-3">
      {socials.map(name => (
        <a href={LINKS[name]} target="_blank" rel="noopener noreferrer"
           class="flex items-center gap-3 text-on-surface-variant hover:text-[var(--ho)] transition-colors duration-300 w-fit"
           style={`--ho: ${ICON_COLORS[name]}`}>
          <Icon name={name} class="w-5 h-5" />
          <span class="font-label text-sm">{name === 'appleMusic' ? 'Apple Music' : name.charAt(0).toUpperCase() + name.slice(1)}</span>
        </a>
      ))}
    </div>

    <p class="font-body text-sm text-on-surface-variant uppercase tracking-widest text-center mt-24">
      &copy; 2026 RIE Music. Sva prava zadr&#x17E;ana.
    </p>
  </div>
</footer>
